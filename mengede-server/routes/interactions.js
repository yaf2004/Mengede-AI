import express from 'express';import{recordInteraction,getUserIntelligence}from '../services/intelligence.js';const r=express.Router();r.post('/',async(req,res)=>{try{const userId=req.header('x-device-id');const{type,entityType,entityId,metadata}=req.body||{};if(!userId||!type)return res.status(400).json({ok:false,error:'x-device-id and type are required.'});res.status(201).json({ok:true,event:await recordInteraction({userId,type,entityType,entityId,metadata})});}catch(e){res.status(500).json({ok:false,error:e.message});}});r.get('/intelligence',async(req,res)=>{try{const userId=req.header('x-device-id');if(!userId)return res.status(400).json({ok:false,error:'x-device-id is required.'});res.json({ok:true,intelligence:await getUserIntelligence(userId)});}catch(e){res.status(500).json({ok:false,error:e.message});}});export default r;