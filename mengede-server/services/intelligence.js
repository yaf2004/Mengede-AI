import {InteractionEvent,UserIntelligence} from '../models/intelligence.js';
export async function recordInteraction({userId,type,entityType,entityId,metadata={}}){if(!userId)throw new Error('userId is required');const event=await InteractionEvent.create({user_id:userId,type,entity_type:entityType,entity_id:entityId,metadata});const add={};if(entityType==='university'&&entityId)add.explored_universities=entityId;if(entityType==='pathway'&&entityId)add.explored_pathways=entityId;if(type==='RESOURCE_SAVED'&&entityId)add.saved_resources=entityId;if(type==='RESOURCE_DISMISSED'&&entityId)add.rejected_resources=entityId;const map={UNIVERSITY_SAVED:'university_interest:',PATHWAY_ACCEPTED:'pathway_interest:',RESOURCE_COMPLETED:'resource_completion:',RESOURCE_SAVED:'resource_interest:'};const key=map[type]?(map[type]+entityId):null;const update={$setOnInsert:{user_id:userId}};if(Object.keys(add).length)update.$addToSet=add;if(key)update.$push={signals:{key,value:true,strength:type==='RESOURCE_COMPLETED'?.85:.7,evidence:[{type:'interaction',source:type,created_at:new Date()}]}};await UserIntelligence.findOneAndUpdate({user_id:userId},update,{upsert:true,new:true});return event;}
export async function getUserIntelligence(userId){return UserIntelligence.findOne({user_id:userId}).lean();}
