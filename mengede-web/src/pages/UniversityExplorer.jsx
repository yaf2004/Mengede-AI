import{useEffect,useState}from'react';import{Link,useParams}from'react-router-dom';import{getResources,getUniversity,recordInteraction}from'../lib/api.js';import ResourceCard from'../components/ResourceCard.jsx';import{Icon}from'../lib/icons.jsx';export default function UniversityExplorer(){const{slug}=useParams();const[data,setData]=useState(null),[resources,setResources]=useState([]),[error,setError]=useState('');useEffect(()=>{Promise.all([getUniversity(slug),getResources({university:slug})]).then(([u,r])=>{if(!u.ok)setError(u.error||'University not found.');else{setData(u.university);recordInteraction('UNIVERSITY_EXPLORED','university',slug);}if(r.ok)setResources(r.resources);});},[slug]);if(error)return <div className="card p-6 text-rose-600">{error}</div>;if(!data)return <div className="card p-6">Loading university exploration…</div>;return <div><Link to="/" className="text-sm text-blue-600 font-semibold">← Back</Link><div className="card p-6 mt-4"><div className="flex items-start gap-4"><div className="w-14 h-14 rounded-2xl bg-blue-50 text-blue-600 flex items-center justify-center"><Icon name="graduation-cap" className="w-7 h-7"/></div><div><h1 className="text-2xl font-extrabold">{data.name}</h1><p className="text-slate-500 mt-1">{data.city} · {data.region} · {data.type}</p><p className="mt-3 text-slate-600">{data.description}</p></div></div></div><section className="mt-6"><h2 className="text-lg font-bold mb-3">Departments to explore</h2><div className="flex flex-wrap gap-2">{(data.departments||[]).map(d=><span key={d} className="pill glass" style={{cursor:'default'}}>{d}</span>)}</div></section><section className="mt-8"><h2 className="text-lg font-bold mb-1">See what it is actually like</h2><p className="text-sm text-slate-500 mb-3">Videos and resources are curated here so you can investigate the university without starting from a blank search.</p>{resources.length?<div className="grid md:grid-cols-2 gap-4">{resources.map((r,i)=><ResourceCard key={r.external_id||r._id||i} resource={r}/>)}</div>:<div className="card p-5 text-sm text-slate-500">No curated media yet. Configure YOUTUBE_API_KEY for live video discovery.</div>}</section>{data.website&&<a href={data.website} target="_blank" rel="noreferrer" className="inline-flex mt-6 text-sm text-blue-600 font-semibold">Official university information →</a>}</div>}